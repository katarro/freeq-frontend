export interface Service {
  id: string;
  branchId: string;
  serviceTypeId: string;
  name: string;
  queueId: string;
  currentExecutiveId: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
}
